                           </div>
                           <div className="p-2 space-y-0.5">
@@ -2905,7 +3106,7 @@
                     return (
                       <div key={p.id} className="bg-white rounded-xl border border-gray-100 overflow-hidden shadow-sm hover:shadow-lg hover:-translate-y-0.5 transition-all duration-200 group">
                         <div className="relative cursor-pointer overflow-hidden" onClick={() => openProduct(p)}>
-                          <img src={p.image} alt={p.name} className="w-full h-36 object-cover group-hover:scale-105 transition-transform duration-300" />
+                          <img src={p.image} alt={p.name} loading="lazy" decoding="async" className="w-full h-36 object-cover group-hover:scale-105 transition-transform duration-300" />
                           {discount > 0 && (
                             <span className="absolute top-2 left-2 bg-[#EE4D2D] text-white text-[9px] font-bold px-1.5 py-0.5 rounded-md shadow-sm">-{discount}%</span>
                           )}
@@ -3028,6 +3229,64 @@
                     </div>
                   ) : null}
 
+                  {multiVariantList.length > 1 && (
+                    <div className="border border-gray-100 rounded-xl p-3 sm:p-4 space-y-3 bg-gray-50/60">
+                      <div className="text-gray-700 text-[12px] font-semibold">Chọn số lượng theo từng phân loại</div>
+                      <div className="space-y-2 max-h-72 overflow-y-auto pr-0.5">
+                        {multiVariantList.map((v: any) => {
+                          const label = selectedVariantInfo.isGrouped ? (comboAttrLabel(v.attributes, selectedVariantGroupNames) || v.name) : v.name;
+                          const outOfStock = Number(v.stock || 0) <= 0;
+                          const qty = multiVariantQtyOf(v.id);
+                          const showOwnPrice = Number(v.price) !== Number(selectedProduct?.price || 0);
+                          return (
+                            <div key={v.id || v.name} className={`flex items-center justify-between gap-2 rounded-lg border px-3 py-2 ${outOfStock ? 'bg-gray-100 border-gray-100' : 'bg-white border-gray-200'}`}>
+                              <div className="min-w-0 flex-1">
+                                <div className="text-[11px] font-medium text-gray-700 truncate">{label}</div>
+                                <div className="flex items-center gap-2 mt-0.5">
+                                  {showOwnPrice && <span className="text-[#EE4D2D] text-[11px] font-semibold">{formatVND(v.price)}</span>}
+                                  {outOfStock ? (
+                                    <span className="text-red-400 text-[10px] font-medium">Hết hàng</span>
+                                  ) : (
+                                    <span className="text-gray-400 text-[10px]">Kho: {v.stock}</span>
+                                  )}
+                                </div>
+                              </div>
+                              <div className="flex items-center border border-gray-200 rounded-lg overflow-hidden flex-shrink-0">
+                                <button
+                                  type="button"
+                                  onClick={() => setMultiVariantQtyFor(v, qty - 1)}
+                                  disabled={outOfStock}
+                                  className="px-2 py-1.5 hover:bg-gray-50 transition-colors disabled:opacity-30"
+                                ><Minus size={11} /></button>
+                                <input
+                                  type="number"
+                                  inputMode="numeric"
+                                  min={0}
+                                  max={v.stock}
+                                  step={1}
+                                  value={qty}
+                                  disabled={outOfStock}
+                                  onChange={(e) => setMultiVariantQtyFor(v, e.target.value)}
+                                  className="w-12 text-center text-xs py-1.5 outline-none disabled:bg-gray-100 [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none"
+                                />
+                                <button
+                                  type="button"
+                                  onClick={() => setMultiVariantQtyFor(v, qty + 1)}
+                                  disabled={outOfStock}
+                                  className="px-2 py-1.5 hover:bg-gray-50 transition-colors disabled:opacity-30"
+                                ><Plus size={11} /></button>
+                              </div>
+                            </div>
+                          );
+                        })}
+                      </div>
+                      <div className="flex items-center justify-between pt-2 border-t border-gray-200 text-[11px]">
+                        <span className="text-gray-500">Tổng số lượng: <span className="font-semibold text-gray-700">{multiVariantTotalQty}</span></span>
+                        <span className="text-gray-500">Tạm tính: <span className="font-bold text-[#EE4D2D]">{formatVND(multiVariantTotalPrice)}</span></span>
+                      </div>
+                    </div>
+                  )}
+
                   <div className="flex items-center gap-3">
                     <span className="text-gray-500 text-[11px] font-medium">Số lượng</span>
                     <div className="flex items-center border border-gray-200 rounded-lg overflow-hidden">
@@ -3040,18 +3299,43 @@
 
                   <div className="flex gap-3 pt-2">
                     <button
-                      onClick={() => { addToCart(selectedProduct, selectedVariant, selectedQty); showToast('Đã thêm vào giỏ hàng!'); flyToCart(productImgRef.current); }}
-                      disabled={displayStock === 0 || variantChoiceDisabled}
+                      onClick={() => {
+                        if (multiVariantActive) {
+                          multiVariantSelectedLines.forEach(({ v, qty }) => addToCart(selectedProduct, v.name, qty));
+                          setMultiVariantQty({});
+                          showToast('Đã thêm vào giỏ hàng!');
+                          flyToCart(productImgRef.current);
+                          return;
+                        }
+                        addToCart(selectedProduct, selectedVariant, selectedQty);
+                        showToast('Đã thêm vào giỏ hàng!');
+                        flyToCart(productImgRef.current);
+                      }}
+                      disabled={purchaseActionDisabled}
                       className="flex-1 border-2 border-[#EE4D2D] text-[#EE4D2D] py-2.5 rounded-lg font-bold flex items-center justify-center gap-2 hover:bg-[#FFF4F1] transition-colors disabled:opacity-40 disabled:hover:bg-white"
                     >
                       <ShoppingCart size={14} /> Thêm Vào Giỏ Hàng
                     </button>
                     <button
-                      onClick={() => { if (!currentUser) { showToast('Vui lòng đăng nhập để thanh toán'); openAuthModal('login'); return; } setBuyNowItem({ productId: selectedProduct.id, variant: selectedVariant, qty: selectedQty }); setBuyerPage('checkout'); window.scrollTo?.({ top: 0 }); }}
-                      disabled={displayStock === 0 || variantChoiceDisabled}
+                      onClick={() => {
+                        if (!currentUser) { showToast('Vui lòng đăng nhập để thanh toán'); openAuthModal('login'); return; }
+                        if (multiVariantActive) {
+                          setBuyNowItems(multiVariantSelectedLines.map(({ v, qty }) => ({ productId: selectedProduct.id, variant: v.name, qty })));
+                          setBuyNowItem(null);
