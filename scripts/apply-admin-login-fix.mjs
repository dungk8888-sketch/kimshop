import { readFileSync, writeFileSync } from 'node:fs';

const path='src/App.tsx';
let s=readFileSync(path,'utf8');
const start=s.indexOf('  const doLogin = async () => {');
const end=s.indexOf('\n\n  const doRegister = async () => {', start);
if(start<0 || end<0) throw new Error('[admin login fix] doLogin anchors missing');

const block=`  const doLogin = async () => {\n    if (loginBusy) return;\n    const uname=authForm.username.trim().toLowerCase();\n    if(!uname||!authForm.password){\n      const msg='Vui lòng nhập tài khoản và mật khẩu';\n      setLoginError(msg); showToast(msg); return;\n    }\n    setLoginBusy(true);\n    setLoginError('');\n    try {\n      let data:any=null;\n      let error:any=null;\n\n      if (uname === 'admin') {\n        // Preview-only fallback: route admin password auth through same-origin Vercel\n        // to avoid browser -> Supabase timeout paths. Password is still verified by Supabase.\n        const r=await fetch('/api/admin-login',{\n          method:'POST', headers:{'content-type':'application/json'},\n          body:JSON.stringify({password:authForm.password})\n        });\n        const body=await r.json().catch(()=>({}));\n        if(r.ok && body?.access_token && body?.refresh_token){\n          const sess=await supabase.auth.setSession({access_token:body.access_token,refresh_token:body.refresh_token});\n          data=sess.data; error=sess.error;\n        }else{\n          error={message:body?.error||'auth_failed',status:r.status};\n        }\n      } else {\n        const first=await supabase.auth.signInWithPassword({email:usernameToEmail(uname),password:authForm.password});\n        data=first.data; error=first.error;\n        if ((error || !data?.user) && !uname.includes('@')) {\n          const legacy=await supabase.auth.signInWithPassword({email:usernameToLegacyEmail(uname),password:authForm.password});\n          data=legacy.data; error=legacy.error;\n        }\n      }\n\n      if(error||!data?.user){\n        const text=String(error?.message||'').toLowerCase();\n        const transient=text.includes('timeout')||text.includes('network')||text.includes('fetch')||Number(error?.status)>=500;\n        const msg=transient ? 'Supabase Auth đang timeout. Hãy thử lại sau ít phút.' : 'Sai tài khoản hoặc mật khẩu';\n        setLoginError(msg); showToast(msg); return;\n      }\n      setLoginError('');\n      setAuthModal(null);\n      showToast('Đăng nhập thành công!');\n    } catch (e) {\n      console.error('Đăng nhập thất bại', e);\n      const msg='Không thể kết nối để đăng nhập. Vui lòng thử lại.';\n      setLoginError(msg); showToast(msg);\n    } finally {\n      setLoginBusy(false);\n    }\n  };`;

s=s.slice(0,start)+block+s.slice(end);
writeFileSync(path,s,'utf8');
console.log('[KIMSHOP FIX] preview admin auth routed through same-origin Vercel fallback');
